# Issue Log - {{PROJECT_NAME}}

> **역할**: 작업 중 발견한 이슈, 버그 원인, 해결 이력을 누적 기록합니다.
> **읽기 시점**: 버그 수정, 장애 대응, 유사 이슈 조사 시 읽습니다.
> **기록 규칙**: 새 항목은 **맨 위에** 추가합니다 (날짜 역순).
> 반복되는 패턴이 확인되면 [decisions.md](file://./decisions.md)로 승격합니다.

---

<!-- 아래 예시를 참고하여 새 이슈를 기록하세요. 예시는 삭제하지 않아도 됩니다. -->

<!-- 예시:

## 2026-05-30 | Gemini

### [Frontend / UI] AI 버튼이 특정 레이아웃에서 미노출
- **현상**: `detail` 레이아웃을 사용하는 페이지에서 AI 플로팅 버튼이 보이지 않음
- **원인**: 해당 버튼은 `default.vue` 레이아웃에만 정의되어 있었음
- **해결**: `detail.vue`에도 `<AiFloatingButton />` 컴포넌트를 추가

### [Deployment / Infra] connex-fe 이미지 버전 불일치
- **현상**: 소스코드는 최신이나 Pod에 AI 버튼이 없음
- **원인**: K8s에서 구동 중인 이미지가 기능 추가 전 빌드본(04-27)이었음. Registry 푸시 누락.
- **해결**: Jenkins에서 새 태그로 빌드 후 푸시, `helm upgrade` 실행

-->
